schemaVersion: 1
name: g2rain-infra-app
family: frontend-app
role: g2rain 平台基础设施管理微前端应用
version: 0.1.0
license: Apache-2.0

architectureBaseline:
  repository: https://github.com/g2rain/g2rain
  ref: architecture-v1.1.0
  profile: docs/architecture/profiles/frontend-app
  version: 1.0.0
  status: planned
  releasedAt: "2026-08-25"
  deviations: docs/architecture/deviations.md

ownership:
  owns:
    - 发号器、字典用途、字典项、地区语言和国际化消息的管理端页面与交互用例
    - 本应用的路由注册、资源加载、权限呈现、国际化和微前端生命周期
    - 本项目页面代码生成器和资源配置生成器
  excludes:
    - g2rain-infra 的领域数据、持久化、编号算法和服务端校验
    - IAM 的认证、Token 签发和会话规则
    - Gateway 的鉴权、路由和请求转发
    - main-shell 的全局菜单、Tab 和子应用注册治理

businessDomains:
  - distributed-id-management
  - dictionary-management
  - locale-settings
  - internationalized-messages

toolchain:
  node: ">=22"
  packageManager: npm
  lockfileVersion: 3
  framework: Vue 3.5.26
  language: TypeScript 5.9.3
  bundler: Vite 7.3
  microFrontend: qiankun 2.10.16
  ui: Element Plus 2.13
  state: Pinia 3

layers:
  order: [shared, components, platform, runtime, views]
  targetDependencyDirection: views -> runtime -> platform -> components -> shared
  compositionRoots: [src/main.ts, src/App.vue]

runtime:
  applicationCode: g2rain-infra-app
  contextPath: /infra
  modes:
    alone:
      selector: mode=alone 或 VITE_RUN_MODE=alone
      auth: 应用自行执行 IAM SSO、Token 管理和资源加载
    integrated:
      selector: 默认空模式
      auth: main-shell 通过 qiankun props 注入 Token、Client、Locale、初始路由和 appKey
  resourceEndpoint: /basis/authority/resources
  backendRoutePrefix: /infra
  devServer:
    configuredPort: 3000
    viteFallbackPort: 3001
  container:
    exposedPort: 8080
    defaultServerPort: 80

commands:
  install: npm ci --legacy-peer-deps
  dev: npm run dev
  verify: npm run build
  preview: npm run preview
  codegen: npm run build:generate -- --tables=<table>
  resourceConfig: npm run build:config
  image: docker build --build-arg VITE_BUILD_MODE=production -t g2rain/g2rain-infra-app:<tag> .

generators:
  code:
    input: src/shared/generator/database.sql
    output: src/views/<table>
    updatesRouteMap: true
    overwritesExistingFiles: true
    switches: [--no-view, --no-api, --no-mock, --no-route]
  resource:
    routeInput: src/views/route-map.ts
    viewInput: src/views
    output: src/shared/config-util/config
    generated: [resources.json, pages.json, page-elements.json]
    apiEndpointGeneration: false

collaborators:
  - repository: g2rain/g2rain-main-shell
    role: 统一入口、Tab、路由同步和 qiankun 装载
  - repository: g2rain/g2rain-iam
    role: SSO、Token 和客户端认证能力
  - repository: g2rain/g2rain-gateway-webflux
    role: 后端业务 API 的统一入口和鉴权转发
  - repository: g2rain/g2rain-basis
    role: 应用页面、页面元素与 API 资源下发
  - repository: g2rain/g2rain-infra
    role: 发号器、字典、地区语言和国际化消息后端能力

validation:
  reviewedAt: "2026-09-05"
  command: npm run build
  status: build-passed
  testsRun: 0
  notes:
    - 依赖按 package-lock.json 同步后，Vue TypeScript 检查和 Vite 生产构建通过
    - 仓库未配置 test 或 lint 脚本，也未发现自动化测试文件
    - 构建仍存在循环分块、MockJS eval、经典 env-config.js 和大包体积警告

documentation:
  entry: docs/index.md
  agentEntry: AGENTS.md
  required:
    - docs/project.yaml
    - docs/community.md
    - docs/architecture/overview.md
    - docs/architecture/layers.md
    - docs/architecture/dependencies.md
    - docs/architecture/runtime-flows.md
    - docs/architecture/deviations.md
    - docs/development/local-development.md
    - docs/development/code-conventions.md
    - docs/development/views-conventions.md
    - docs/development/components-and-platform.md
    - docs/development/code-generation.md
    - docs/development/resource-generation.md
    - docs/development/testing.md
    - docs/development/definition-of-done.md
    - docs/development/git-workflow.md
    - docs/operations/configuration.md
    - docs/operations/deployment.md
    - docs/operations/troubleshooting.md
    - docs/security/security-boundaries.md
    - docs/requirements/README.md

